# Basic Mosquitto configuration for this project
persistence true
persistence_location /mosquitto/data/
log_dest file /mosquitto/log/mosquitto.log

# Default listener on standard MQTT port
listener 1883

# Allow anonymous (change for production if needed)
allow_anonymous true
